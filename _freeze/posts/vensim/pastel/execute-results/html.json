{
  "hash": "fbd39386d69446d7b81b46230ae9c352",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: Modelo del pastel\nformat: html\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\npacman::p_load(dplyr, ggplot2, patchwork)\n```\n:::\n\n\n\n\nSe horneo un delicioso “pie” de nuez para hacer este estudio.\nAl finalizar la cocción se sacó del horno un apetitoso “pie” de color\ndorado intenso, al que se le tomó cuidadosamente la temperatura al instante\nde salir del horno y luego cada minuto durante una hora.  Estos datos son los\nque se anotan a continuación.\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nTemperatura <- c(90.2, 90, 87, 81, 78, 75, 71.5, 68.5, 65, 63.5, 61, 60, 58, 56.5,\n                 55, 54, 52, 50.5, 50, 48.8, 47)\n```\n:::\n\n\nUn tema importante para utilizar datos experimentales en la construcción de modelos es la capacidad de leer y escribir a archivos externos.  A continuación les presento una forma sencilla de como puede hacerse esto. Para escribir la ruta pueden optar por usar la representación \"\\\\\" de las diagonal o substituirla po \"/\", resultan equivalentes en este contexto. El uso de dobles diagonales es el formalismo \"oficial\", pero la otra diagonal es muy práctica.  También noten que estoy utilizando la notación \"punto\" para referirme al\ndirectorio de trabajo como sitio de inicio en la especificación de la ruta.\n\n\nPara guardar imágenes en un archivo hay varias opciones. Sugiero se consulte help(bitmap) para ver como hacerlo.\n\nLos siguientes comandos les serán útiles:\n\n+ `getwd()` este es el directorio de trabajo que está activo.\n+ `list.files()` para ver los archivos en ese directorio.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndatos.guardados <- cbind(sec=seq(from=0, to=100, by=5),\n                           Temperatura)\n\nwrite.table(datos.guardados, row.names = FALSE,\n            file = \"sistemas/datos del pastel.txt\")\n\ntemp.leida <- read.table(header = TRUE,\n      file=\"sistemas/datos del pastel.txt\")\n\npastel <- data.frame(tiempo=seq(from=0, to=100, by=5), Temperatura=Temperatura)\n```\n:::\n\n\n\nGráfica del proceso de enfriamiento. Les sugiero adoptar, en lo posible, el [enfoque de _tubería_ ](https://www.datacamp.com/tutorial/pipe-r-tutorial. Esta metáfora sugiere que el proceso de interés ocurre a lo largo de un recorrido por el _tubo_, así que lo único que hay que especificar son esos procesos. La forma de usar esta metáfora es vincular cada evento con el símbolo `|>` (también puede ser %>% si recurren a las bibliotecas `tidyverse` o `dplyr`, aunque opera con algunas diferencias). En este caso usaremos el que ha adoptado **R** como base. La función `plot` básica es bastante \"antigua\" en *R* ni incluye un argumento que tome los `datos` en conjunto, opera sobre variables separadas. Por eso usaremos la función `width` que permite agregar esa información de referencia al proceso.\n\n\n::: {.cell}\n\n```{.r .cell-code}\npastel |> \n  with(plot(tiempo, Temperatura))\n```\n\n::: {.cell-output-display}\n![](pastel_files/figure-html/unnamed-chunk-4-1.png){width=672}\n:::\n:::\n\n\nLa aproximación que usaremos ahora utiliza lo que se llama el \"cociente de diferencia central\".  Para hacerlo tomaremos dos versiones de los datos desfasados convenientemente para dejar un intervalo que empata pares de temperatura entre t-1 (usaremosla función `lag` de `dplyr`) y t+1 (usaremos la función `lead`) y asegura que la longitud total de los nuevos datos sea congruente, en este caso perdemos dos puntos en la serie. En la metáfora de tubos, utilizaremos la función `mutate` para hacer cambios a los datos.\n\n\n::: {.cell}\n\n```{.r .cell-code}\npastel <- pastel |> \n  mutate(dif_central = (lead(Temperatura, 1) - lag(Temperatura, 1)) / (lead(tiempo, 1) - lag(tiempo, 1)))\n```\n:::\n\n\nVeamos ahora como se ven estos datos. Usaremos ahora una mejor forma de graficació, que incluye muchas más opciones y por tanto puede resultar algo más compleja. Se trata de la biblioteca `ggplot2`. Por favor observen que el uso de `ggplot` recurre al signo \"+\" para concatenar los elementos que requerimos para construir la  gráfica que deseamos, componentes que incluyen tipos de gráficas (geoms), etiquetas (labs) y temas (themes), entre otras posibilidades. En el cálculo de la diferencia central aparecieron algunoas celdas sin datos (\"na\"). Conviene omitir esas filas en el proceso siguiente. Usaremos la función `filter` para indicarle que **no queremos ninguna fila en la que `dif_central`** tenga \"na\". Lo averiguamos con la función `is.na` e indicamos que queremos la condición contraria anteponiendo el símbolo \"!\" (suele indicar negación en *R*). \n\nEsta es la gráfica de la evolución de la temperatura en el tiempo.\n\n\n::: {.cell}\n\n```{.r .cell-code}\npastel |> \n  filter(!is.na(dif_central)) |> \n  ggplot(aes(x = tiempo, y = dif_central)) +\n  geom_point() +\n  labs(main = \"Diferencia central vs. tiempo\") +\n  xlab(label = \"tiempo (min)\") +\n  ylab(label = \"dT/dt\")\n```\n\n::: {.cell-output-display}\n![](pastel_files/figure-html/unnamed-chunk-6-1.png){width=672}\n:::\n:::\n\n\nEsta otra es la comparación de la temperatura en cada momento y la magnitud en la que ha cambiado para llegar a ese momento desde el inmediato anterior. Por cierto el carácter con número hexadecimal \"U00B0\" es el símbolo de grados en el sistema de codificación **UTF-8**.\n\n\n::: {.cell}\n\n```{.r .cell-code}\npastel |> \n  filter(!is.na(dif_central)) |> \n  ggplot(aes(x = Temperatura, y = dif_central)) +\n  geom_point() +\n  labs(main = \"Diferencia central vs. temperatura\") +\n  xlab(label = paste0(\"temperatura \\U00B0\", \"C\")) +\n  ylab(label = \"dT/dt\")\n```\n\n::: {.cell-output-display}\n![](pastel_files/figure-html/unnamed-chunk-7-1.png){width=672}\n:::\n:::\n\n\n\nEn el caso de nuestros datos la Ec. 7.2 puede convenirnos escribirla como una\nfunción que nos permita hacer los cálculos respectivos en forma sencilla.\nUtilizaremos los datos siguiente: T0 = 90.2, TAmb= 23.5 con esta información la\necuación del pastel toma la forma siguiente. La forma como propongo escribir los\nargumentos de la función es para dar el valor que tomarán cuando se omita\nproporcionar esta información.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nec_pastel <- function (t, alfa, TAmb = 23.5, T0 = 90.2)\n                        {TAmb + exp(t * alfa) * (T0-TAmb)}\n```\n:::\n\n\nque es una ecuación que es función del tiempo \"t\" y que tiene como\nincógnita a \"alfa\".  Nuestro interés es ahora encontrar alguna manera de evaluar\nque tanto puede usarse esta ecuación para explicar el caso particular que\nnos interesa. La correspondencia entre la propuesta teórica y los datos obtenidos\npuede valorarse buscando la mejor aproximación que podamos obtener entre los\nvalores que produce la ecuación propuesta y los que obtengamos experimentalmente.\nEn el caso de nuestro pastel, una aproximación cruda pero interesante se obtiene\nal tomar digamos el dato a los 20 minutos y resolviendo la ecuación para\ntener el valor de \"alfa\" que corresponde con los datos proporcionados.\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nalfa_1 <- log((78 - 23.5)/66.7) / 20\n```\n:::\n\n\nSólo resta substituir el dato de \"alfa\" en la ecuación y ver que tanto empatan los\nvalores que produce con los datos derivados de nuestra muestra. La calidad del\najuste puede estimarse calculando la suma de las desviaciones absolutas entre el\nvalor predicho y el observado.  Una forma de hacer esto es calcular la suma de\ncuadrados de esta diferencia la que guardaremos en \"sc_modelo_1\".\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsc_modelo_1 <- with(data = pastel, expr = sum ((Temperatura - ec_pastel (tiempo, alfa_1))^2))\nsc_modelo_1\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 62.8706\n```\n\n\n:::\n:::\n\n\nOtra posible aproximación es considerar que si cambiamos la variable temperatura\npor la diferencia con la temperatura ambiente (23.5 en nuestro ejemplo), tenemos\nuna expresión lineal que puede estimarse como una regresión simple.  Esto lo\nharemos para obtener la segunda estimación de alfa que guardaremos en \"alfa_2\".\nPara hacer esto\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\npastel$dif_temp <- pastel$Temperatura - 23.5\nalfa_2 <- coefficients(lm(dif_central ~ dif_temp - 1, data=pastel))[[1]]\n```\n:::\n\n\n\n¿Cómo se ve el ajuste de esta ecuación?  Podemos ver el ajuste tanto en el espacio\nde la tasa de cambio como en el comportamiento de la temperatura del pastel a lo\nlargo del tiempo.  También podemos comparar el grado de ajuste de las dos\nestimaciones de alfa que tenemos.  Será mejor tanto más pequeña sea la suma de\ncuadrados entre los valores ajustados y los observados.\nLa diferencia en el ajuste de ambos propuestas se ve claramente en una gráfica.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsc_modelo_2 <- with(pastel, sum((Temperatura - ec_pastel (tiempo, alfa_2))^2))\nc(alfa1=sc_modelo_1, alfa2=sc_modelo_2)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   alfa1    alfa2 \n62.87060 43.89377 \n```\n\n\n:::\n\n```{.r .cell-code}\npastel |> \n  filter(!is.na(dif_central)) |> \n  ggplot(aes(x = dif_temp, y = dif_central)) +\n  geom_point(colour = \"red\") +\n  scale_x_continuous(limits = c(20, 100)) +\n  scale_y_continuous(limits = c(-1, 0)) +\n  labs(main = \"Diferencia Central vs. Temperatura\") +\n  xlab(label = paste0(\"temperatura \\U00B0\", \"C\")) +\n  ylab(label = \"dT/dt\") +\n  geom_abline(slope = alfa_1, colour = \"green\", linewidth = 1.5) +\n  geom_abline(slope = alfa_2, colour = \"blue\", linewidth = 1.5)\n```\n\n::: {.cell-output-display}\n![](pastel_files/figure-html/unnamed-chunk-12-1.png){width=672}\n:::\n:::\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\npastel |> \n  ggplot(aes(x = tiempo, y = Temperatura)) +\n  geom_point(color = \"red\") +\n  geom_line(aes(y = ec_pastel(alfa_1, tiempo)), color = \"green\", linewidth = 1.5) +\n  geom_line(aes(y = ec_pastel(alfa_2, tiempo)), color = \"blue\", linewidth = 1.5)+\n  scale_x_continuous(limits = c(0,100)) +\n  scale_y_continuous(limits = c(20,100)) +\n  xlab(label = \"tiempo (min)\")+\n  ylab(label = \"temperatura °C\")\n```\n\n::: {.cell-output-display}\n![](pastel_files/figure-html/unnamed-chunk-13-1.png){width=672}\n:::\n:::\n\n\n\n\nSi quiero guardar la gráfica final puedo hacerlo con el comando `ggsave`. Si no generé un \n*objeto* para conservar el objeto gráfico, entonces el comando buscará en la memoria un objeto apropiado, normalmente encontrará el último que hiciste. La extensión que pongas al nombre del archivo selecciona también el tipo de archivo a guardar.\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggsave(filename = \"resultados pastel.png\")\n```\n\n::: {.cell-output .cell-output-stderr}\n\n```\nSaving 7 x 5 in image\n```\n\n\n:::\n:::\n\n\nSi quieren organizar la figura como un conjunto de gráficas, les sugiero utilizar la biblioteca `patchwork`. Con esta función un conjunto de objetos gráficos separados por el símbolo \"+\" se acomodan en un renglón y para hacer un nuevo renglón basta con poner el símbolo \"/\"\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ng1 <- pastel |> \n  filter(!is.na(dif_central)) |> \n  ggplot(aes(x = dif_temp, y = dif_central)) +\n  geom_point(colour = \"red\") +\n  scale_x_continuous(limits = c(20, 100)) +\n  scale_y_continuous(limits = c(-1, 0)) +\n  labs(main = \"Diferencia Central vs. Temperatura\") +\n  xlab(label = paste0(\"temperatura \\U00B0\", \"C\")) +\n  ylab(label = \"dT/dt\") +\n  geom_abline(slope = alfa_1, colour = \"green\", linewidth = 1.5) +\n  geom_abline(slope = alfa_2, colour = \"blue\", linewidth = 1.5)\n\ng2 <- pastel |> \n  ggplot(aes(x = tiempo, y = Temperatura)) +\n  geom_point(color = \"red\") +\n  geom_line(aes(y = ec_pastel(alfa_1, tiempo)), color = \"green\", linewidth = 1.5) +\n  geom_line(aes(y = ec_pastel(alfa_2, tiempo)), color = \"blue\", linewidth = 1.5) +\n  scale_x_continuous(limits = c(0,100)) +\n  scale_y_continuous(limits = c(20,100)) +\n  xlab(label = \"tiempo (min)\")+\n  ylab(label = \"temperatura °C\") \n    \n    \ng1 + g2\n```\n\n::: {.cell-output-display}\n![](pastel_files/figure-html/unnamed-chunk-15-1.png){width=672}\n:::\n:::\n",
    "supporting": [
      "pastel_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}